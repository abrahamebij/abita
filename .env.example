# Deployer / keeper wallet private key (MetaMask → export private key)
PRIVATE_KEY="0x..."

# Deployed AbiCore contract address — update after every redeploy
# Used by: scripts/keeper.ts
ABICORE_ADDRESS="0x..."